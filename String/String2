class Solution {
    int isPalindrome(String S) {
     
       for(int i=0;i<S.length();i++){
           if(S.charAt(i)==S.charAt(S.length()-1-i)){
               continue;
           }
           else
          {
            return 0; 
          }
       }
       return 1;
    }
}
